// Change this only after the owner approves the actual public copy and policies.
// See docs/OPERATIONS.md. Configuration alone must not publish draft terms.
export const productionContentApproved = false;
